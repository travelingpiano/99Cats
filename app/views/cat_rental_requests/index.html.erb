<h1> All Rental Requests</h1>

<ul>
  <% @requests.each do |request| %>
  <li> <%= request.cat.name %>
    <%= request.status %>
    <%= link_to "#{request.cat.name}\'s Rental Request Page", cat_rental_request_url(request) %>
  </li>
  <% end %>
</ul>
